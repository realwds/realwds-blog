#article-container
  if top_img === false
    h1.page-title= page.title

  != page.content

  .timeline(class="notice")
    if site.data.notice
      each i in site.data.notice
        .timenode
          .meta
            p
            p=i.date
            p
          .body
            p=i.msg
