nav#nav
  div#blog_name
    a#site-name(href=url_for('/')) #[=config.title]
  //- div#page_name
  //-   a#page-name-text(href=url_for('/')) #[=page.title || page.tag || page.category || config.title]

  #menus
    if (theme.algolia_search.enable || theme.local_search.enable)
      #search-button
        a.site-page.social-icon.search
          i.fas.fa-search.fa-fw
          span=' '+_p('search')
    #darkmode_navswitch
      a.darkmode_switchbutton(onclick='navFn.switchDarkMode()')
        i.fas.fa-adjust      
    !=partial('includes/header/menu_item', {}, {cache: true})

    #toggle-menu
      a.site-page
        i.fas.fa-bars.fa-fw


